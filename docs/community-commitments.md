# 對外承諾追蹤表

> 在 issue／PR 留言裡說過「會做」「正在修」的事與交付狀態。**代發任何含承諾的留言後立刻更新；發版前清一遍「未交付」段。**
> 病根：拍板後排到「之後」就蒸發（#56、#10 都發生過；#66 的返工指引草稿曾空等 58 天）。
> 判準：承諾前先驗前提；公開留言不寫機制，只寫「確認了／方向找到／排在哪之後／過渡方法」。

更新：2026-09-20（#70 回音推翻右 Alt 計畫前提＝回報者用的是 Hold；新開 #79／#80 與 #70 疑為同一根因；#30 兩位回報者結果相反；#77 今日到期）

## 🔴 未交付

| 串 | 說過什麼 | 現況 | 下一步 |
|---|---|---|---|
| #70 #72 Windows 自動貼上 | 「下一版最優先修」 | v0.12.0 已修（還原 v0.10.0 探測時序，成因為候選）、已請實測 | 等回報者回音；若仍不貼上，重新找成因 |
| #70 問題 2 Groq 沒 AI 整理 | 「還在查」，請換 GPT OSS 120B 試 | 回報者 9/11：實為 HUD 沒出現、v0.12.1 後 OK | 已解 |
| #70 右 Alt 放開後焦點跳走（9/11 留言） | 2026-09-13 回問「選單列是否反白」，說「我知道怎麼處理，會排進下一個小版本」 | **2026-09-14 回音：動工前提（通過判準）未達成**。回報者用 Hold（計畫表格第三列已涵蓋 Hold，**成因推理未被推翻**），但他沒做判準要求的對照：判準要「不點輸入框、不按 Esc 直接 Ctrl+V」，他第 4 題答的是「必須再點一下對話框」。Edge 正常、Claude Code 對話框跳選單。**他已自行改用「右 Alt＋.」繞過、稱「這個就沒什麼問題了」**。`docs/plan-windows-alt-menu-mask.md` 計劃閘三輪通過、計畫仍有效，只是不能動工 | 與 #79／#80 併案設計：**這串和那兩串是同一時序取捨的兩端**——v0.12.0 為修本串把探測提前，換來 #79／#80；壓回去只會讓本串復發。主要候選＝UIA 讀選取、不模擬按鍵。#70 本身痛點已被回報者繞過，單獨優先度可降 |
| #79 obxyann：Hold＋Alt／Shift 選取後不進編輯模式（9/14 新開） | 未留言 | Windows 11、v0.14.1。Hold 左右 Alt 或 Shift 時選取文字只走一般辨識；**Hold Ctrl 正常**；Toggle 下 Alt 仍不行、Ctrl／Shift 時好時壞。回報者另註 UpNote 裡 Hold Alt 反而可以。🔬 **讀碼確認（未實機驗證）：這是已知取捨的另一端代價，不是新 bug**。`read_selection_state` 註解明寫 Windows 在「錄音開始時」當場送 Ctrl+C，並註明改成「按鍵放開後才送」會讓 #70／#72 回歸。於是實體 Alt 仍壓著時目標程式實收 `Alt+Ctrl+C`（非複製）→ 讀不到選取。Toggle 下「有時好有時壞」＝探測與手指離鍵之間隔一次非同步 IPC 的競態 | 與 #70／#80 併案設計，主要候選＝UIA 讀選取（註解自己寫「待 UIA 版補上」、本就在內部待辦）；回串稿待使用者過目 |
| #80 obxyann：Hold＋Alt 觸發會先多打一個 `C`（9/14 新開，回歸） | 未留言 | Windows 11、v0.14.1。Line／WeChat 輸入框 Hold Alt 會先出現 `C`、Hold Shift 出現兩個反引號、PowerShell 出現 `^C`；**Toggle Alt 與 Hold Ctrl 無事**。與 #79 同一根因（見上列）。回報者稱「之前解決的在 0.14.1 又出現」——確實如此，v0.12.0 改時序時換來的 | 同上；這是使用者直接看得到的字元污染，優先度高於 #70 |
| #73 HackMD 誤讀 | Windows 換新讀取方式仍在計劃、短期還會在 | 需 Windows 實機 | 與 #30 同批 |
| #74 廣東話選項 | 「排在修完 Windows 貼上之後」 | **v0.13.0 已交付（2026-09-11 公開）** | 回串（稿已擬，等使用者放行）：提醒用內建整理模式與預設 whisper-large-v3 |
| #76 啟動時隱藏主視窗 | 「排在修完 Windows 貼上之後」 | **v0.12.1 已交付（2026-09-11 公開）**，已回串通知 | 等回報者回音後關 |
| Windows 左 Ctrl 觸發鍵（LINE 回報，無 issue） | 對回報者說會修 | **v0.12.1 已交付**（`docs/plan-windows-injected-key-guard.md`）；#70／#72 已補一句 | 私訊請實測 Ctrl Hold／Toggle、編輯模式、自訂鍵（#30） |
| #71 模型清單標「需付費」 | 「之後會標更清楚」 | **v0.13.0 已交付（2026-09-11 公開）** | 回串（稿已擬，等使用者放行） |
| #30 Windows 自訂觸發鍵、右 Alt／右 Ctrl | 兩次誠實進度留言 | **v0.14.0 已交付、chenjhchi 2026-09-12 確認正常**；HUD 跟隨螢幕 2026-09-13 拍板**不做**、已回串請實測（issuecomment-5653326946）。**兩位回報者結果相反**：chenjhchi 9/13 說滑鼠移到副螢幕 HUD 仍不出現（未報版本）、cruelforever 9/14 以 v0.14.1 測三種配置全部正確跟著滑鼠 | 向 chenjhchi 問版本與螢幕配置（主副／縮放）；釐清前不能判定「Windows 不跟滑鼠」成立，也還不能關單 |
| #78 AmberCTW、#70（9/11 留言）錄製時視窗在前景錄不到 | 未留言（拍板直接修） | **v0.14.1 已交付（2026-09-12 公開）**（`docs/plan-hotkey-recording-focus-fallback.md`；DOM 補錄限 Windows）；已回串 #78（issuecomment-5646012475）、#70（issuecomment-5646012983） | 等回報者 Windows 實測 Alt+Z、右 Alt＋右 Ctrl 回音 |
| #66 lettucebo Azure | 9/24 無回音就接手（rebase 到最新 main、Azure 拿掉 temperature），併入後通知 #45 | 等作者 | 9/24 檢查 |
| #67 lettucebo 設定備份 | 2026-09-11 已留言請作者 rebase 到 v0.12.0、剝掉字典半邊；CI 綠後單獨審 file_transfer | 等作者 | 作者回音後審；與 #66 同人，9/24 一起看 |

## ⏳ 待發出

| 串 | 條件 |
|---|---|
| #24 cocomanbo Obsidian（7/14 後 61 天未回） | 2026-09-13：Heptabase 句中觸發**不重現**（未進編輯模式、直接貼上）；路徑與 Obsidian 不同（Obsidian 走 AX selection→LLM 失敗）。2026-09-13 已回串請回報者更新 v0.14.1、開除錯記錄重現並附上（issuecomment-5653367327）；等記錄 |

## ⚪ 等外部、無行動項

#6（等 Microsoft 審查）、#77（**2026-09-20 到期**：回報者自 9/10 起未再回，可關閉並註明補齊資訊可重開——留言待使用者放行）、#36 第二問題（已關，補資訊重開）、#5（說設定同步未排程，沒承諾做）、#75 自訂錄音路徑（只說記下來）。

## ✅ 已銷帳

- 2026-09-13：#42／#75 留言後關閉；#70 回問（issuecomment-5652908237）
- 2026-09-12 v0.14.1：#78／#70 回串（焦點時錄不到已修）
- 2026-09-10 v0.12.0：#61／#60／#58／#41 併入通知並關閉；#40／#5／#42／#75 交付通知；#70／#72 請實測
- 2026-09-10：#70/#72/#73/#74/#75/#76/#71/#77/#30/#36/#41/#66/#60/#58 共 14 則進度留言；#36 關閉
- 2026-07-12 v0.11.0：#43/#39/#37/#38/#24/#36/#56/#10 九串通知

## 內部待辦（無對外承諾）

FK 787 調查、Windows edit-mode UIA 升級、#24「編輯失敗退回貼上」、ElevenLabs 三項 HIGH（見 `plan-elevenlabs-stt-provider.md`）。
